A simple NX project with Angular Frontend and NestJs Backend.

install all dependencies with `npm i`

Then run backend with `npm run backend`
Then run frontend with `npm run frontend`

And you are ready to go.

If you want to run tests, simple run `npm run backend-tests` or `npm run frontend-tests`
